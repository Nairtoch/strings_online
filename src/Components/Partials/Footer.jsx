import FooterImage from '../../Assets/Images/footer.png';

export const Footer = () => {
    return(
        <section className='Footer'>
            <img src={FooterImage} alt="Image for the footer." />
        </section>
    )
}